spring:
  datasource:
    url: jdbc:mysql://localhost:3306/testdb-test
    username: root
    password: ${PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop